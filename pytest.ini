[pytest]
# pytest 를 스크립트로 직접 실행하면 리포 루트가 sys.path 에 들어가지 않아
# `from app.schemas import ...` 가 ModuleNotFoundError 로 깨진다.
# `python -m pytest` 로는 재현되지 않으므로 로컬에서만 통과하는 함정이었다.
pythonpath = .
testpaths = tests
